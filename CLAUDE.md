# Claude Code Notes

@AGENTS.md

Use `AGENTS.md` as the shared source of project instructions. This file exists
only to make Claude Code load those shared instructions and to hold any future
Claude Code-specific addendum.

No Claude Code hooks, subagents, or custom commands are required for the initial
`circuit` scaffold.
